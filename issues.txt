the app isn't responsive at so many places, when I am in edit mode and press the add button in the bottom it only shows the ui, nothing is clickable when clicked the screen goes away, I also want to be able to adjust the transparency and other things in the settings, @liquidGL-main use this to implement liquid glass where necessary, this ui feels ai generated, not apple designed, refine proportions and hierarchy, reduce corner radius, avoid big pill shaped cards and buttons, Use tighter spacing and subtle contrast, make it calm and intentional, like a pro macos app, not a generic saas dashboard, fix radii, spacing, sizing, proportions, tighten up the spacing with a clear 8-point system, and more negative space, consistent line height, less bold text, subtle borders instead of heavy shadows, animation should feel purposeful and springy not constant, and icons should be slightly, smaller and precisely aligned, define a token set for radius, padding, gaps, every component should follow it, replace the current auto with a freeform canvas, users can move freely, but there is an invisible 8 or 12 pixel grid just for subtle magnetic snapping, not forced slots or auto rearranging, make the snapping soft, so user can override it by dragging a little further, show alignment guides when edges or center lines up, similar to figma or keynote, and don't reflow other widgets automatically, free resizing with a corner handle, replace the small, medium, large widget resizing with freeform resizing via a subtle bottom right handle, use an invisible 8 pixel grid with soft snapping and alignment guides; no auto reflow. add keboard support: arrow keys to nudge, shift+arrow for larger steps, option+arrow to resize. show clear focus state, bounding box and size readout for accessibility; prioritize performance and clean geometry, add a responsive ios mode: when the viewport is phone-sized, automatically switch to ios styling - tab bar navigation, sf typography, ios spacing and safe area insets. keep feature parity, don't just scale down the mac layout, make the desktop layout feel Macbook-Native: finder-style sidebar and top toolar, sf typography and symbols, subtle dock at the bottom with spring animations, rstrained vibrancy on key surfaces. in phone sized viewports, automatically switch to an ios look - tab bar or nav bar patterns, sf fonts, ios spacing and safe area insets, keep feauture parity, don't just scale down the mac layout, make the entire app fully responsive and platform-aware. on desktop, use a macos feel for shell and widgets. on phone viewports, use an ios feel. not just the outer shell, but every widget and embedded app must reflow and adapt. use fluid grids, container queries where needed, and logical breakpoints so components rearrange or stack gracefully instead of just scaling,. respect min and max sizes, touch target spacing on mobile, safe areas, and key test flows on macos and ios breakpoints, 